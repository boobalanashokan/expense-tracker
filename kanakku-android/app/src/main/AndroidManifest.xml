<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kanakku"
        android:usesCleartextTraffic="false">

        <!-- ── Main App Activity ── -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- ── Quick Add Activity (launched from widget) ── -->
        <activity
            android:name=".QuickAddActivity"
            android:exported="true"
            android:theme="@style/Theme.Kanakku.Dialog"
            android:windowSoftInputMode="adjustResize">
        </activity>

        <!-- ── Small Widget 2x2 ── -->
        <receiver
            android:name=".widget.KanakkuWidget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
                <action android:name="com.kanakku.WIDGET_REFRESH"/>
                <action android:name="com.kanakku.QUICK_ADD"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info"/>
        </receiver>

        <!-- ── Wide Widget 4x2 ── -->
        <receiver
            android:name=".widget.KanakkuWidgetWide"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
                <action android:name="com.kanakku.WIDGET_REFRESH"/>
                <action android:name="com.kanakku.QUICK_ADD"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_wide_info"/>
        </receiver>

        <!-- ── Background data refresh service ── -->
        <service
            android:name=".widget.WidgetUpdateService"
            android:exported="false"/>

    </application>
</manifest>
